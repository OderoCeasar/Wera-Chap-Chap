root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/wera-api ."
bin = "./tmp/wera-api"
include_ext = ["go"]
# bin/ holds `make build` output and db/sqlc is generated -- neither should
# trigger a rebuild loop.
exclude_dir = ["tmp", "bin"]
